name: etl_manager_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+3

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2
  go_router: ^17.2.1
  dio: ^5.9.2
  flutter_secure_storage: ^10.0.0
  url_launcher: ^6.3.2 
  google_fonts: ^6.2.1  
  image_picker: ^1.1.2
  intl: ^0.19.0
  flutter_image_compress: ^2.3.0
  image: ^4.5.4
  path_provider: ^2.1.4
  cached_network_image: ^3.4.1
  local_auth: ^2.3.0
  shared_preferences: ^2.3.3
  camera: ^0.12.0+1
  geolocator: ^14.0.1
  flutter_map: ^8.1.1
  latlong2: ^0.9.1

  # ⚠️ Declared EXPLICITLY on purpose. core/services/sse_service.dart imports
  # `package:http`, but until now it was only ever pulled in TRANSITIVELY by
  # another package. Adding the Firebase packages below re-resolves the
  # lockfile, and if nothing else happened to depend on http any more, SSE
  # would break with a compile error. Never rely on a transitive import.
  http: ^1.6.0

  # ─── Push notifications ───────────────────────────────────────────────────
  # Background + terminated delivery is drawn by the native FCM SDK, so no
  # local-notifications package is required for the case that actually matters.
  #
  # Foreground is handled per-platform without an extra dependency:
  #   iOS     — setForegroundNotificationPresentationOptions() makes the system
  #             draw the banner even while the app is open.
  #   Android — the OS deliberately does NOT draw one for a foreground app, so
  #             PushService shows an in-app banner and refreshes the badge.
  #
  # `flutter_local_notifications` was considered for Android foreground banners
  # but deliberately left out for now: v21+ forces compileSdk 36 and minSdk 24
  # (this app currently inherits Flutter's defaults and targets minSdk 21 per
  # flutter_launcher_icons below), which is a device-support decision rather
  # than a notifications one. Easy to add later if system-style foreground
  # notifications are wanted.
  firebase_core: ^4.12.1
  firebase_messaging: ^16.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.13.1
  riverpod_generator: ^4.0.3
  json_serializable: ^6.13.0
  freezed: ^3.2.5
  # Generates all Android + iOS launcher icon sizes from the source PNGs.
  # Run once after adding the logo:  dart run flutter_launcher_icons
  flutter_launcher_icons: ^0.14.4

# ─── App launcher icon (ETL "Eat Truck Love" logo) ───────────────────────────
# Drop the logo files under assets/icon/ (see assets/icon/README.md), then run:
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  image_path: "assets/icon/app_icon.png"          # 1024x1024, square
  android: true
  ios: true
  remove_alpha_ios: true                            # App Store: no transparency
  background_color_ios: "#FFFFFF"
  min_sdk_android: 21
  adaptive_icon_background: "#FFFFFF"               # Android 8+ adaptive icon
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icon/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

// staff_checklist_screen.dart
import 'package:flutter/material.dart';

class StaffChecklistScreen extends StatelessWidget {
  const StaffChecklistScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Checklist')));
}
